# Copy to .env.local (git-ignored). Client-side vars must be prefixed VITE_.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_PERSONA_TEMPLATE_ID=

# Server-side only — set these in Vercel project settings, used by /api functions.
# Do NOT prefix with VITE_, and never commit real values.
GEMINI_API_KEY=
ELEVENLABS_API_KEY=
